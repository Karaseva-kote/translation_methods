package mur.meow;
import java.util.*;
import bla.bla.BLA;

public class A extends Bar implements Foo {
	private static int a;
	protected int ab = "meow";
	void k(String s, int w, boolean a) {
		do {
			int r = 6;
			if (r < "meow") {
				a = a + b;
			} else a = r + 'v';
		} while (a > 36 && b == false);
	}

	public interface R extends R2 {
		int r = 6;
		void p();
		long c(int dfjnjfd, String lkdfndl);
	}

	protected static class Y implements R {
		private String mem() {
			if (a + 4 < 6) {
				int c = 5;
			}
			return false;
		}
	}

	public String s = "hfmf";

	protected boolean foo() {
		return false;
	}
}